import React from 'react'

const Button = () => {

  let moveUp = () => {

  }
  
  return (
    <button
      title={title}
      onClick={onClick}
    >
    {title}
    </button>
  )
}

export default Button